const { name, version } = require('../../package.json')

const owner = 'ThreeHero'

const gitRepoNames = {
  vue: 'yj-template-vue',
  react: 'yj-template',
  springboot: 'yj-template-springboot'
}

module.exports = {
  name,
  version,
  owner,
  gitRepoNames
}
